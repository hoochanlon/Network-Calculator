# 网络计算器 (Network Calculator)

一个现代化的网络计算器应用，提供专业的 IP 地址计算、子网划分、路由聚合等功能，采用 Flutter 开发，支持 Windows 桌面和 Web 平台。

**语言 / Language / 言語**: [简体中文](README.md) | [English](README_EN.md) | [日本語](README_JA.md)

![Version](https://img.shields.io/badge/version-1.6.0-blue.svg)
![Flutter](https://img.shields.io/badge/Flutter-3.0+-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

## ✨ 功能特性

### 核心计算功能

- **IP 地址计算器** - 计算 IP 地址的网络信息、广播地址、可用主机数等
- **子网掩码计算器** - 根据主机数或子网掩码计算网络参数，支持二进制和十六进制显示
- **IP 进制转换器** - IP 地址在二进制、十进制、十六进制之间的转换
- **路由聚合计算器** - 合并多个子网为 CIDR 地址块，支持两种聚合算法
- **超网拆分计算器** - 将大地址块拆分为多个小网段
- **IP 包含检测器** - 检测 IP 地址或网段是否包含在另一个网段中

### 用户体验

- 🎨 **现代化 UI** - 采用网易云音乐风格的界面设计
- 🌓 **主题切换** - 支持亮色/暗色主题，多种颜色主题可选
- 🌍 **多语言支持** - 简体中文、繁体中文、英文、日文
- 📝 **历史记录** - 自动保存计算历史，支持搜索、导入、导出
- 🔄 **状态保存** - 自动保存输入状态，切换页面不丢失数据
- 📱 **响应式设计** - 适配不同屏幕尺寸，侧边栏宽度自适应
- 🎯 **侧边栏排序** - 支持拖拽排序和项目锁定功能

## 🛠️ 技术栈

- **框架**: Flutter 3.0+
- **状态管理**: Provider
- **本地化**: flutter_localizations + intl
- **数据存储**: SharedPreferences + 文件系统
- **UI 组件**: Material Design 3
- **图标**: Material Symbols Icons

## 📋 环境要求

- Flutter SDK >= 3.0.0
- Dart SDK >= 3.0.0
- Windows 10/11 (桌面版)
- 现代浏览器 (Web 版)

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone <repository-url>
cd Network-Calculator
```

### 2. 安装依赖

```bash
flutter pub get
```

### 3. 运行项目

**桌面版 (Windows):**
```bash
flutter run -d windows
```

**Web 版:**
```bash
flutter run -d chrome
```

### 4. 构建发布版本

**Windows 桌面应用:**
```bash
flutter build windows --release
```

**Web 应用:**
```bash
flutter build web --release
```

## 🔧 构建说明

### Windows 桌面构建

1. **确保 Flutter 环境配置正确**
   ```bash
   flutter doctor
   ```

2. **构建 Release 版本**
   ```bash
   flutter build windows --release
   ```

3. **输出位置**
   - 构建产物位于: `build\windows\x64\runner\Release\`
   - 可执行文件: `network_calculator.exe`

### Web 构建

1. **构建 Web 版本**
   ```bash
   flutter build web --release
   ```

2. **输出位置**
   - 构建产物位于: `build\web\`
   - 可直接部署到 Web 服务器

### 使用 Inno Setup 创建安装包

项目包含 Inno Setup 脚本，可以创建 Windows 安装程序：

```bash
# 运行 Inno Setup 构建脚本
.\InnoSetup.bat
```

## ❗ 常见问题

### Flutter 构建失败

#### 1. 依赖问题

**问题**: `flutter pub get` 失败或依赖冲突

**解决方案**:
```bash
# 清理缓存
flutter clean
flutter pub cache repair

# 重新获取依赖
flutter pub get
```

#### 2. Windows 插件构建失败

**问题**: Windows 平台插件编译错误

**解决方案**:
```bash
# 运行修复脚本
.\fix_windows_plugins.ps1
# 或
.\fix_windows_plugins.bat

# 重新构建
flutter build windows --release
```

#### 3. CMake 错误

**问题**: CMake 配置或编译失败

**解决方案**:
```bash
# 清理构建缓存
flutter clean
rmdir /s /q build\windows

# 重新构建
flutter build windows --release
```

#### 4. 符号链接问题 (Windows)

**问题**: 构建时出现符号链接相关错误

**解决方案**:
```bash
# 运行符号链接修复脚本
.\fix_symlink_issue.ps1
# 或
.\fix_symlink_issue.bat
```

#### 5. 本地化文件生成失败

**问题**: `flutter pub get` 后本地化文件未生成

**解决方案**:
```bash
# 手动生成本地化文件
flutter gen-l10n

# 或重新运行
flutter pub get
```

#### 6. Web 平台构建失败

**问题**: Web 构建时出现错误

**解决方案**:
```bash
# 清理 Web 构建缓存
flutter clean
rmdir /s /q build\web

# 检查 Web 支持
flutter config --enable-web

# 重新构建
flutter build web --release
```

### 运行时问题

#### 1. 历史记录无法导入/导出 (Web 平台)

**问题**: 在 Chrome 浏览器中导入历史记录后出现乱码

**解决方案**: 
- 确保 JSON 文件使用 UTF-8 编码
- 已修复：使用 `utf8.decode()` 正确解码文件内容

#### 2. 侧边栏拖拽排序不生效

**问题**: 长按上锁/解锁功能无效

**解决方案**: 
- 需要在设置中启用"侧边栏拖拽排序"功能
- 只有启用拖拽排序后，长按上锁/解锁才会生效

#### 3. 主题切换不生效

**问题**: 切换主题后界面没有变化

**解决方案**:
```bash
# 重启应用
# 或清理缓存后重新运行
flutter clean
flutter run
```

## 📁 项目结构

```
lib/
├── core/                    # 核心功能
│   ├── models/              # 数据模型
│   ├── providers/           # 状态管理
│   ├── services/           # 业务逻辑服务
│   ├── theme/               # 主题配置
│   └── utils/               # 工具类
├── l10n/                    # 本地化文件
│   ├── app_zh.arb           # 简体中文
│   ├── app_zh_TW.arb        # 繁体中文
│   ├── app_en.arb           # 英文
│   └── app_ja.arb            # 日文
└── ui/                      # UI 界面
    ├── screens/             # 页面
    └── widgets/             # 组件
```

## 🎨 自定义配置

### 修改侧边栏宽度

编辑 `lib/ui/screens/main_screen.dart`:

```dart
// 默认宽度：240，大屏幕（宽度 > 1600）最大宽度：270
final sidebarWidth = screenWidth > 1600 ? 270.0 : 240.0;
```

### 添加新的计算器

1. 在 `lib/core/services/` 创建服务类
2. 在 `lib/ui/screens/calculator_screens/` 创建界面
3. 在 `lib/ui/screens/main_navigation_items.dart` 注册

### 添加新语言

1. 在 `lib/l10n/` 创建 `app_xx.arb` 文件
2. 运行 `flutter gen-l10n` 生成代码
3. 在 `lib/main.dart` 的 `supportedLocales` 中添加

## 📝 开发指南

### 代码规范

项目使用 `flutter_lints` 进行代码检查，确保代码质量：

```bash
# 运行代码检查
flutter analyze
```

### 本地化开发

1. 修改 `lib/l10n/app_zh.arb` (模板文件)
2. 同步更新其他语言文件
3. 运行 `flutter gen-l10n` 生成代码

### 测试

```bash
# 运行单元测试
flutter test

# 运行集成测试
flutter test integration_test
```

## 🤝 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 🙏 致谢

- [Flutter](https://flutter.dev/) - 跨平台 UI 框架
- [Material Symbols Icons](https://fonts.google.com/icons) - 图标库
- 所有贡献者和用户的支持

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- 提交 [Issue](https://github.com/your-repo/issues)
- 发送邮件

---

**注意**: 本项目仍在积极开发中，如有问题请及时反馈。

